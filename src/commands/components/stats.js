module.exports = {
	help: {
		name: "stats",
		description: "Stats",
		slash: false,
		category: "components",
		aliases: ["stats"],
		usage: "stats",
		cooldown: 5,
		deletemsg: true,
	},
	run: async(client, message, args) => {

		// client.updateStats(message.guild);

	},
};